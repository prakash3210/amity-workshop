# requirements.txt
# Pin to a stable MCP SDK version. The `mcp` package includes:
#   • mcp.server  — server-side classes and decorators
#   • mcp.client  — client-side session and transport helpers
#   • mcp.types   — shared data-model types (Tool, TextContent, …)
mcp>=1.0.0

# Required for server_standalone.py (HTTP transport) and clientstandalone.py:
#   • uvicorn  — ASGI server for server_standalone.py
#   • starlette — Web framework for Streamable HTTP (included by mcp; listed for clarity)
uvicorn>=0.31.0
starlette>=0.27.0
